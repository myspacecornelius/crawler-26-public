# ──────────────────────────────────────────────────
#  CRAWL — Dependencies
# ──────────────────────────────────────────────────

# Core
playwright>=1.40.0
beautifulsoup4>=4.12.0
pyyaml>=6.0
pandas>=2.1.0

# Async HTTP and SMTP
aiohttp>=3.9.0
aiosmtplib>=3.0.0

# Email validation (optional, for MX lookups)
dnspython>=2.4.0

# ── SaaS Backend ──
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0  # passlib 1.7.4 is incompatible with bcrypt>=5 (missing __about__)
python-multipart>=0.0.6

# Rate limiting
slowapi>=0.1.9

# Database migrations
alembic>=1.13.0

# Stripe billing
stripe>=15.1.0

# Task queue (optional — degrades gracefully)
celery[redis]>=5.3.0

# Google Sheets integration (optional)
# gspread>=6.0.0
# google-auth>=2.25.0

# Airtable integration (optional)
# pyairtable>=2.2.0

# ── Testing ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
